"use client";
import styled from '@emotion/styled';
import { Typography } from '@mui/material';

export const LeagueLogo = styled('img')`
 max-width: 25px;
 border-radius: 50%;
`;

export const LeagueTitle = styled(Typography)`
 color: blue;
 padding-right: 10px;
`;
